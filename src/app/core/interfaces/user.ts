export interface User {
  id: number;
  email: string;
  password: string;
  pixKey: string;
  firstName: string;
  lastName: string;
  imageURL: string;
  imageBase64: string;
  themeColor: string;
}
